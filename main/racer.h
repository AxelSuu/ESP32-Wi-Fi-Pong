#pragma once

#include "game_module.h"

// Racer — single-player top-down dodge-the-traffic game steered by tilt
// (INPUT_TILT) with left/right buttons as a fallback. See racer.c for tuning.
extern const game_module_t RACER;
